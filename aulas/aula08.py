import random
import math
num = random.randint(1, 10)
print(f"O número que a máquina escolheu foi {num}")
raisquadrada = int(input("Digite um número: "))
raiz = math.sqrt(raisquadrada)
print(f"A raiz quadrada de {raisquadrada} é igual á {raiz}")
