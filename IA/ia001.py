import numpy as np
# É uma biblioteca que trabalha com arrays (tipo listas, mas mais poderosas).
# Fazer cálculos direto nela, tipo multiplicar tudo por 2 de uma vez só
lista = np.array([2,4,6])
print(lista * 3) # Saída: [ 6 12 18]

